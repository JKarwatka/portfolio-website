import { SvgIcon } from "@mui/material";
import styled from "styled-components";

export const StyledLogo: typeof SvgIcon = styled(SvgIcon)({
  background: "transparent",
  width: "48px",
  height: "48px",
});
