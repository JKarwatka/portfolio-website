import { Box } from "@mui/system";
import styled from "styled-components";

export const HeroSectionBackground = styled(Box)`
  min-height: 100vh;
  background: url("images/hero-background.jpg") no-repeat right center;
  background-size: 100%;
`;
